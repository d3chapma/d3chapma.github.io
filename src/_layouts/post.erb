---
layout: default
---

<% index = collections.posts.resources.map(&:data).find_index(data) %>

<%= render "post_nav", index: index, date: data.date %>

<h1><%= data.title %></h1>

<%= yield %>

<%= render "post_nav", index: index, date: data.date %>
